# Copy this to .env and set your production/dev values. Do NOT commit your real keys.
GOOGLE_PLACES_API_KEY=YOUR_GOOGLE_PLACES_API_KEY_HERE

# When running Netlify locally with netlify dev, Netlify will pick up .env and set them for functions too.

